import sdl, input;
import signal;
import std.map, std.conv;
import std.array, std.stdio;

def main () {
    let win = initSDL ("Test", 800, 600);
    while (!poll ()) {
    	win.clear ();
    	win.swapWindow ();
    }
}
