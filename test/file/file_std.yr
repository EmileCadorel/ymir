import std.math;
import std.stdio._;

def main (files) {
    let file = fopen (files [1], "r");
    let line = fread (file, 1024u);
    
    println (line);
    
    fclose (file);    
    printf ("func : %.12f, cpu : %.12f\n", sqrt (1234, 7), (1234.).sqrt);

    return 0;
}



