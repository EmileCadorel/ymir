import std.stdio.file;
import std.stdio.print;

def main () {
    let file = fopen ("Makefile", "r");
    let line = fread (file, 1024);

    println (line);
    
    fclose (file);    
    return 0;
}



