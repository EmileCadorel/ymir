import std.stdio;
import std.string;

def test (a : function (string, [int]) : int) {
    return ('salut').a ([2, 4]);
}

def main () {    
    println (test (function (a : string, b : [int]) : int {
		println (a);
		let tot = int.init;
		for (it in b) {
		    tot += *it;
		}
		return tot;
    }));    
}
