import std.stdio;

def foo (T) (a : function  (T, T) : T) : T{
    return a (1, 2);
}

def foo (T) (a : [T]) {
}    

def main () {
    println (foo (function (a : int, b : int) : int {
		return (a + b) * 10;
	    }));

    foo ([1, 2, 3]);    
}

