import std.stdio;
import std.string;


def test (A : string, B) (a : B) {
    if (A.equals ("salut")) {
	println ("Bonjour");
    } else {
	println ("Au revoir");
    }
}

def main () {
    let a = ("salut").equals ("h");
    test!("salut", float) (10.);
    test!("u") (11);
    test!("salut") (123);
}
