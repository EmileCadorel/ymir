
def print_int(num) : void {
    let a = num;
    if (num < 0) {
	system (print_c, '-');	
	print_int (- num);
    } else {  
	if (num > 9)
	    print_int (num / 10);
       	system (print_c, ((a % 10) + '0'));
    }
}

def main () {
    let a = -1030;
    print_int (a);
}
