
def println (s : string) {
    let i = 0;
    while (i < s.length) {
	system (print_c, s[i]);
	i++;
    }
    system (print_c, '\n');
}

struct (a : B) A;

struct (a : A) B;

def main () {
    let a = A (B (null));
    if (a.a is null)
	println ("ici");
    else
	println ('la');
}
