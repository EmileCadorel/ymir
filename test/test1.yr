import std.stdio;
import std.string;

struct
| a : int
-> test;

def main () {
    let a = test (10);
    a = null;
    if (a !is null && a.a == 10) {
	println ("ici");
    } else {
	println ("ouf");
    }

    if (a is null || a.a == 10) {
	println ("ouf");
    } else {
	println ("ici");
    }
}
