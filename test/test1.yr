import std.stdio;
import std.math;


def printTab (a : [int], sep : string) {
    print ('[');
    for (it in 0 .. a.length) {
	print (a [it]);
	if (it != a.length - 1)
	    print (sep);
    }
    println (']');
}

def main () {
    printTab ([1l, 2, 3, 4], " : ");
}

