extern (C) putchar (c : char);

def test (c) return cast:string ([c]);

def print (s : string) {
    for (it in s)
	putchar (it);
}

def main ( ) {
    let a = test ('r');
    print ("salut");
    for it in a 
	print(it.typeid);
}

