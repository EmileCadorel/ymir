extern (C) putchar (c : char);

struct (f : int, s : long, t : string) A;

struct (nb : int, tab : array!int) B;


def print (i : long) : void {
    if (i < 0) {
	system (print_c, '-');
	print (- i);
    } else {
	if (i > 9) print (i / 10);
	system (print_c, (i%10) + '0');
    }
}

def print (c : char) {
    putchar (c);
}

def print (i : int) : void {
    if (i < 0) {
	system (print_c, '-');
	print (- i);
    } else {
	if (i > 9) print (i / 10);
	system (print_c, (i%10) + '0');
    }
}

def println (i) {
    print (i);
    putchar ('\n');
}

def print (s : string) {
    let i = 0;
    while i < s.length {
	    print (s [i]);
	    i++;
    }
}

def print (a : array!int) {
    let i = 0;
    print ('[');
    while (i < a.length) {
	print (a[i]);
	if (i < a.length - 1) print (", ");
	i ++;
    }
    print (']');
}

def main () {
    let b = B (10, []);
    b.tab += [10, 12];
    println (b.tab);
}
