
def fibo (n : int) {
    if (n < 2) return n;
    else return fibo (n - 1) + fibo (n - 2);
}

def main () {
    let a = fibo (7);
    while (a > 0) {
	let b = a % 10;
	system (print_c, b + '0');
	a /= 10;
    }
}
