import std.stdio;
import std.array;

def main () {
    let a = [int; 0u];
    for (it in 0 .. 10) {
	a.insertBack (it);
    }
    
    println (a);
    println (a.removeBack (), " ", a.dup (), ' ', a.back ());
    println (a.slice (4ul .. 0ul));
}
