import std.stdio;
import std.array;
import std.conv;

def main () {
    let a = [int; 0u];
    for (it in 0 .. 10) {
	a.insertBack (it);
    }
    
    println (a.to!string);
    println (a.removeBack (), " ", a.dup (), ' ', a.back ());
    println (a.slice (4, 0ul));
}
