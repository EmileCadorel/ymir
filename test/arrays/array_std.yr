import std.stdio;
import std.array;
import std.conv;

def main () {
    let a = [int; 0u];
    for (it in 0 .. 10) {
	a.insertBack (it);
    }
    
    println (a.to!string);
    let b = a.removeBack ();
    println (b);
    println (a.slice (4, 0ul).to!string);
}
