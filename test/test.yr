import std.stdio;
import std.map;
import std.algorithm;
import std.string;
import std.random;
import std.array;

def main () {
    let a = ({
	    void test (int a, int b) {		
		return 10;			
	    }					
	});
    
    a.splitLines
	.map((a : string) => strip (a))
	.each ((a : string) {
		println (a);
	}
    );   
}
