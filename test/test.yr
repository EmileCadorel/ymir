
enum A {
    TEST : 9.,
    FOO : 12.
}

def test (a : A) {
    match a {
        A::FOO  => println ("ici");
        _ => println ("la");
    }
}

def main () {
    let a = A::TEST + A::FOO;
    test (A::TEST);
    test (A::FOO);
}
