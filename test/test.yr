import std.stdio;

def test (op : string) (a : int, b : int) {
    if (op == "+") {
	return a + b;
    } else if (op == "-") {
	return a - b;
    }
    return 0;
}

def main () {
    let a = ("salut" != "salut");
    println (a);
}


