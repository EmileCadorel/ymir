import std.stdio;
import std.map;
import std.algorithm;
import std.random;

def test (n : int) () : void {
    static if (n > 0)
	test! (n - 1)();
    else 
	static assert (false);
}

def main () {
    let immutable a = 299;
    test !(a)();
}
