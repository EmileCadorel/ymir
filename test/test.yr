import std.stdio;
import std.conv;
import std.algorithm;

def main () {
    match (
	('salut', 1, 3),
	(a : string) {
	    println (a);
	},
	(a : string, b : int, c : int) {
	    println (a + to!string (b) + to!string (c));
	    return 10;
	}
    );
    
}

