import std.stdio;
import std.map;
import std.algorithm;
import std.random;

def test (n : int) {
    if (n > 0)
	test (n - 1);
    else
	assert (false);
}

def main () {
    let a = 10;
    test (a);
}
