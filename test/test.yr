import std.stdio;
import std.array;
import std.algorithm;

extern (C) printf (str : ptr!char, ...);
extern (C) rand () : int;
extern (C) srand (i : int);
extern (C) time (i : int) : int;


def main () {
    /*srand (time (0));
    let a = [int; 100UL];
    for (i in 0 .. 100) {
	a [i] = rand () % 100;
    }
    println (a.mergeSort ());*/
    let a = [1, 4];
    println (a.length, ' ', cast:long (a.length) / 2L);

    
}


