import std.stdio;
import std.traits;
import std.datetime;
import std.algorithm;
import std.array;


def main () {
    let a = [1, 2, 3];
    println (a.scan (
	function (a : int, b : int) : int{
	    return a + b;
	}	
    ));
}


