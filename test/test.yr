import std.stdio;
import std.conv;
import std.math;

class A (T, U) {
    | a : T
    | b : U
    -> this;
    | b : s
    -> static;

    def test () {
	let a = (this.a + static.b) / this.b;
    }
}

class B(T, U) : A!(U, T) {    
}


def main () {
    
}


		 
