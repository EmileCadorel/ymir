import std.stdio;
import std.string;
import std.array;
import std.conv;

def test if (is (T : float))
    (T) () {
	return 1.0;
    }

def test if (is (T : int) || is (T : long))
    (T) () {
	return 1;
    }

def main () {
    let a = ("salut")[];
    let b = a.to!(string);
    let c = test!(float) ();
    let d = test!(long) ();    
}


