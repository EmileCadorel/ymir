import std.thread;

def test (data : tuple!(int, float, string)) {
    let (a, b, c) = data;
    println ("LANCÉ ", c);
    println (a, ' ', b);    
}

def main () {
    for it in 0 .. 100 {
        let th = thread (&test, 1, 2.3, "salut");
        th.join ();
    }
    
    println ("FIN");
}
