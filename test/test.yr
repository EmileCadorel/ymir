import std.conv;

private struct Singleton {
    a : int,
    b : float
}

static instance : Singleton;

public def singleton () {
    if (instance is null) {
        instance = Singleton (0, 1.2);
    }
    return instance;
}

def main () {
    let a = singleton ();
    println (a);
}
