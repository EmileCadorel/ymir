import std.stdio;
import std.map;
import std.algorithm;
import std.random;

def foo if (is (T : string))
    (T) (a : T) {
    }

extern (C) foo (a : int, b : float) : int;

def foo (a, b) {
    return 10;
}

def main () {
    let a = foo (10);
}
