import std.stdio;
import std.string;
import std.array;
import std.conv;
import std.math;
import std.algorithm.sorting;

def opTest ('==') (a : string, b : char) {
    static assert (false, "ici");
}

def opEquals (a : string, b : char) {
    return a.length == 1UL && a [0] == b;
}

def main () {
    let a = ('a' == "a");
    let c = "a" != 'e';
    println (a, ' ', c);
}


