import std.stdio;
import std.string;
import std.array;
import std.conv;
import std.math;
import std.algorithm.sorting;

def opBinary ('in') (b : char, a : string) {    
    return indexOf (a, b);
}

def main () {
    let a = 'a' in 'salut';
    println (a);
}


