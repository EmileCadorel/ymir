import std.stdio;
import std.conv;
import std.math, std.array;

struct (T)
| a : T
-> Test;

def print (a : Test!int) {
    if (a !is null)
	print (a.a);
}

def main () {
    let a = [Test!int ; 10UL];
    println (a);
    for (it in a) {
	it = Test!int (1);
    }
    println (a);
}




		 
