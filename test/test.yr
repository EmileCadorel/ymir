import std.stdio;
import std.string;
import std.array;
import std.conv;

def test (a : int) () {
    static if (is (a : int)) {
	return true;
    } else
	return false;
}

def main () {
    test!(-1) ();
}


