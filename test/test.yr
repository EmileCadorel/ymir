import std.stdio;
import std.conv;
import std.array;
import std.algorithm;

def uniform () {
    let static init = false;
    if (init == false) {
	init = true;
	println ("Init");
    }
}


def main () {
    for (it in 0 .. 10)
	uniform ();
}


