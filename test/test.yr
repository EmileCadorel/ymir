import std.stdio;
import std.string;
import std.array;
import std.conv;


def to (string) (elem : [char]) {
    return cast:string (elem);
}


def main () {
    let a = "salut";
    let b = (a[]).to!(string);
    println (b);
}


