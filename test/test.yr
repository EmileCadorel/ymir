import std.stdio;
import std.string;
import std.array;
import std.conv;

def test if (is (T : float))
    (T) () {
	println (1);
    }

def test if (is (T : int))
    (T) () {
	println (2);
    }


def main () {
    test!(float) ();
    test!(int) ();
}


